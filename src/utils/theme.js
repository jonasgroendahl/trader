import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#00e676",
      main: "#00c853",
      dark: "#00c853",
      contrastText: "#fff"
    }
  }
});

export default theme;
